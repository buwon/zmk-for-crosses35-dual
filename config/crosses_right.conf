# For split keyboards:
# central이 왼쪽(peripheral) 배터리 잔량을 받아오기는 하되(OLED 표시용),
# 호스트에 별도 BATT 서비스로 노출하지는 않음 -> BlueZ의
# "More than one BATT service exists" 버그로 인한 재연결 방지
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=n
